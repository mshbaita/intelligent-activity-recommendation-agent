name: ChatGPT temporary runner test

on:
  push:
    branches:
      - chatgpt-temp-fetch-nllb
    paths:
      - .github/workflows/chatgpt-temp-fetch-nllb.yml

permissions:
  contents: read

jobs:
  test-runner:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Basic runner test
        run: |
          echo "runner works"
          uname -a
